---
# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
#
# Package manager defaults for OpenBSD
#
########################################################################

repos:
  openbsd_pkg:
    openbsd:
      url: https://cdn.openbsd.org/pub/OpenBSD
      enabled: true

fw_url: http://firmware.openbsd.org/firmware
