---
# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
#
# Package manager defaults for Ubuntu Linux
#
########################################################################

dist_repo_defaults:
  aptitude:
    url: https://mirrors.edge.kernel.org/ubuntu
    security: false

repos:
  aptitude:
    main:
      enabled: true
    restricted:
      enabled: true
    universe:
    multiverse:
