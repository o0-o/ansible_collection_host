{# vim: ts=2:sw=2:sts=2:et:ft=jinja2

Aptitude configuration file

}
{{ header_var }}
{# TODO: replace this with a custom ISC configuration filter #}
{{  pkg_mgr_cfg_var
    | to_json
    | regex_replace('^{', '')
    | regex_replace('}$', ';')
    | regex_replace('"(\w+)": {', '\1 { ')
    | regex_replace('"(\w+)": ', '\1 ')
    | regex_replace(' \["', ' { "')
    | regex_replace('"]', '"}')
    | regex_replace('", "', '"; "')
    | regex_replace('}', '; }') }}
