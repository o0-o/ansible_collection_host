{# vim: ts=2:sw=2:sts=2:et:ft=jinja2

Authorized public SSH keys

#}
{{ header_var }}
{% for key in user_var['ssh']['auth'] | default([]) | unique %}
ssh-{{ key['type'] }} {{ key['pub'] }}
{% endfor %}
